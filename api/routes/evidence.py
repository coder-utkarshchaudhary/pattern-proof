"""
API Endpoints for accesing evidence given audit_id
Tasks:
    1. GET /evidence/{audit_id} -> Return all evidences for an audit.
    2. GET /evidence/{audit_id}/{evidence_id} -> Returns specific finding for an audit.
    3. GET /evidence/{audit_id}/{screenshot_id} -> Return a particular screenshot with corresponding findings.
"""