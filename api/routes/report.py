"""
API Endpoints for report related tasks
Tasks:
    1. GET /reports/{audit_id} -> Return a json of the report.
    2. GET /report/{audit_id}/pdf -> Returns a signed url from the DB to view the report.
"""